# 来造数据库操作指南

适用环境：腾讯云服务器上的 `laizao-local` 项目，PostgreSQL 运行在 Docker 容器中。

## 1. 数据库实际存储在哪里

数据库服务名是 `db`，数据由 Docker 命名卷 `postgres_data` 保存。数据并不在项目代码目录 `/www/laizao-local` 中。

Docker 在服务器上的实际卷目录通常类似：

```text
/var/lib/docker/volumes/laizao-local_postgres_data/_data
```

不要直接修改该目录里的文件；应始终通过 PostgreSQL 命令操作数据。

## 2. 进入数据库

先进入项目目录：

```bash
cd /www/laizao-local
```

当前项目的数据库账号和数据库名称都是 `vibe_market`。进入 PostgreSQL：

```bash
sudo docker compose -f docker-compose.production.yml --env-file .env exec db psql -U vibe_market -d vibe_market
```

成功后会看到类似提示符：

```text
vibe_market=#
```

## 3. 查看所有表

在 PostgreSQL 提示符中执行：

```sql
\dt
```

当前业务表：

| 表名 | 用途 |
| --- | --- |
| `users` | 用户、创作者和管理员账号 |
| `works` | 作品主信息、审核及试玩部署状态 |
| `work_versions` | 作品版本和 ZIP 源文件关联 |
| `listings` | 广场上架、价格和上架状态 |
| `stored_files` | 上传 ZIP、封面等文件元数据 |
| `orders` | 获取或购买作品的订单 |
| `entitlements` | 订单完成后获得的作品使用权 |
| `events` | 浏览、试玩、发布、审核等行为记录 |

## 4. 常用查询

查看用户：

```sql
SELECT id, username, nickname, is_admin, created_at
FROM users;
```

查看作品及审核、试玩部署状态：

```sql
SELECT id, title, review_status, deployment_status, created_at
FROM works;
```

查看广场已上架作品：

```sql
SELECT id, work_id, status, price_cents, created_at
FROM listings;
```

查看订单：

```sql
SELECT id, user_id, listing_id, status, created_at
FROM orders;
```

查看最近行为事件：

```sql
SELECT event_name, user_id, work_id, created_at
FROM events
ORDER BY created_at DESC
LIMIT 20;
```

## 5. 查看表字段

例如查看 `works` 表有哪些字段：

```sql
\d works
```

其他表同理，例如：

```sql
\d users
\d orders
```

## 6. 退出数据库

```sql
\q
```

## 7. 查看当前数据库配置（不要公开密码）

在服务器终端、项目目录中执行：

```bash
grep -E '^(POSTGRES_DB|POSTGRES_USER|POSTGRES_PASSWORD)=' .env
```

当前配置中，`POSTGRES_DB` 和 `POSTGRES_USER` 均为 `vibe_market`。`POSTGRES_PASSWORD` 是敏感信息：不要发到聊天记录、GitHub 或截图中。

## 8. 安全提醒

- 不要直接编辑 `/var/lib/docker/volumes/.../_data` 中的数据库文件。
- 不要把 `.env` 上传到 GitHub。
- 已在截图中暴露过的数据库密码，建议后续更换。
- 正式运营前应增加定期备份，使用 `pg_dump` 导出 SQL 备份文件到 `/www/backups/`。
